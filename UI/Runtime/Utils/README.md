# Utils cho UGUI

[← RiseOn.Utils.UI](../../README.md)

`ImageUtils`, namespace `RiseOn.Utils.UI`:

```csharp
using RiseOn.Utils.UI;

icon.SetColorA(0.5f);  // chỉ đổi alpha của Image.color, giữ nguyên RGB; SetColorR/G/B, SetColor(index, value) tương tự
```
